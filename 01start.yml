- name : install nginx
  hosts: WEB
  become: true
  tasks:
    - name: install nginx
      ansible.builtin.yum:
        name: http
        state: latest

    - name: restart nginx
      ansible.builtin.systemd:
        name: nginx
        state: started
        enable: yes